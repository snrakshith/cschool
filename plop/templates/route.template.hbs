const express = require("express");
const router = express.Router();

const {
  getAll{{name}},
  createNew{{name}},
} = require("../controllers/{{name}}.controller");

router.get("/get-{{name}}", getAll{{name}});

router.post("/create-{{name}}", createNew{{name}});

exports.module = router;
